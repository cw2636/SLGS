port: 7880
rtc:
  tcp_port: 7881
  udp_port: 0
  use_external_ip: false
  node_ip: 127.0.0.1
  stun_servers: []
  force_tcp: true
  enable_loopback_candidate: true

turn:
  enabled: true
  domain: localhost
  tls_port: 0
  udp_port: 3478

keys:
  slgs-lk-key: slgs-lk-secret-change-in-production

logging:
  level: debug

room:
  auto_create: true
  max_participants: 100

# Development: no TLS needed when behind nginx/localhost
